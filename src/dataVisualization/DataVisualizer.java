package dataVisualization;

import javafx.scene.chart.PieChart;
import java.util.List;
import model.Post;  // Import the Post class from the model package

public class DataVisualizer {
    public PieChart generatePieChart(List<Post> posts) {
        // Generate a pie chart based on the list of posts
        PieChart pieChart = new PieChart();
        // Add data to the pie chart
        // You should categorize the posts and populate the pie chart accordingly
        return pieChart;
    }
}
